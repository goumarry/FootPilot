VITE_API_URL=http://localhost:3001
# Google Maps Places Autocomplete (optionnel — activer les adresses sur le planning)
# Créer une clé sur https://console.cloud.google.com/ avec l'API "Places API" activée
VITE_GOOGLE_MAPS_API_KEY=
